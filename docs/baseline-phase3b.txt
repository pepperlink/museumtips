Phase 3b pre-change baseline (recorded before curated data scaffolds)
Date: 2026-09-12

Commands and results:

1) Build
   Command: hugo --minify
   Exit code: 0

2) Page count (from Hugo build table)
   Command: hugo --minify   (Pages row in output)
   NL pages: 97
   EN pages: 95
   Total pages: 192

3) Script tags in built HTML
   Command: find public -name '*.html' -exec grep -c '<script' {} +
   Count: 0

4) WARN lines in build output
   Command: hugo --minify 2>&1 | grep -c '^WARN'
   Count: 0
